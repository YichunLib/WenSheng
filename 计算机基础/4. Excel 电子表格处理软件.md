---
:UID: 241030041049
---

## 1 Excel 基本知识

### 1.1 功能、启动、退出

1. 功能：Excel，建立电子表格，并可以对表中的数据进行计算、处理、统计分析等。
2. 应用：行政管理，股票分析，数据统计分析，财务管理等。
3. 启动：3.1(6种方法)
4. 退出：2.2.4(8种方法)

### 1.2 Excel 窗口

#### 1.2.1 名称框、编辑栏

1. 名称框：
   - 显示活动单元格的位置；
   - 输入单元格名称按下回车键，可快速定位；
   - 选定单元格区域，输入名称，可为单元格区域定义名称
2. 编辑栏：
   - 显示单元格内容；用于输入、编辑常量和公式(不能在单元格里插入图片(表面))
   - 左侧三个按钮：
     1) X 取消 ESC
     2) ✓ 输入 Enter(下) Tab(右) Shift + Enter(上) Shift + Tab(左) ↑ ↓ ← →
     3) f(x) 插入函数

#### 1.2.2 状态栏

1. 视图：
   - Excel & PPT 默认是普通视图
   - Word 默认是页面视图

#### 1.2.3 窗口管理

1. 拆分窗口：可上下拆分、左右拆分
2. 新建窗口：文件名后面加 :1 :2
3. 全部重排窗口：平铺、水平/垂直并排、层叠
4. 并排查看
5. 切换窗口
6. 冻结窗格：首行、首列、冻结拆分窗格(冻结选中单元格上面的行和左侧得列)
7. 隐藏窗口：可取消

### 1.3 Excel 基本概念

1. 工作簿：Excel 文件(.xlsx)
   新建文件：工作簿1(默认有3张工作表；工作表可以存在1-3张)
2. 工作表(Sheet)：1048576(行) x 16384(列)XFD
3. 单元格：存储数据的最小单位
   单元格地址：先列后行 AAB3
   单元格区域：A1:C5
4. 活动工作表、活动单元格都只有一个
5. 填充柄：活动单元格右下角的一个黑方点；鼠标指针悬停在上方，由空心变为黑十字
6. 全选按钮：位于工作表左上角(按 Ctrl + A 不一定能够全选)

---

## 2 工作簿、工作表、单元格 基本操作

### 2.1 工作簿 基本操作

新建、打开(Ctrl+O/Ctrl+F12)、保存(Ctrl+S/Shift+F12)、另存为(F12)、关闭、打印、保护

### 2.2 工作表 基本操作

1. 选定：单击选定一个工作表；Shift选连续，Ctrl不连续；右击选择全部全选
2. 切换：单击切换；Ctrl+Pgup/Ctrl+Pgdn
3. 插入：行和列只能向上和前方插入；Shift+F11(插入工作表)；“插入工作表”按钮(最后)
4. 删除：工作表不能全删，一个工作簿至少应该留有一张工作表
5. 重命名：双击工作表标签(F2 不能使用)
6. 移动、复制：(Shift+)拖动:移动 | Ctrl+拖动:复制；(Ctrl+C/V 不能使用)；对话框：可在打开的不同文件中移动/复制；可勾选建立副本
7. 隐藏：工作表不能全隐藏，一个工作表至少应该留有一张工作表可见；隐藏不等于删除
8. 保护：通过设定密码来保护工作表

说明：
1. 开始选项卡，单元格组(3个命令，4个元素：插入/删除/格式，表/行/列/单元格)
2. 右键工作表标签( #操作/第四章 )
   不可使用常用快捷键
   以上操作不能撤销(除了更改工作表颜色)

### 2.3 单元格 基本操作

#### 2.3.1 编辑单元格

方法：

1. 单击单元格(格表面，无插入点，覆盖)
2. 双击单元格/编辑栏(格内部，有插入点，修改，无图)

说明：

1. 向多个单元格中输入同一内容：
   选择单元格区域，输入内容，Ctrl+Enter

   |             | Word             | Excel            |
   | ----------: | ---------------- | ---------------- |
   |       Enter | 换行             | 下               |
   | Shift+Enter | 插入手动换行符   | 上               |
   |  Ctrl+Enter | 插入手动分页符   | 批量填充         |
   |   Alt+Enter | 重复插入上一个词 | 单元格内手动换行 |

   

2. 输入数据：
   数据分类：常量、公式(=开头)

   - 常量：1)数值型 2)字符型 3)日期和时间 4)逻辑型
     1) 数值型: 右对齐
        - 分数: 整数 n/m (如 0 1/2)
        - 负数: -5或(5)
        - 浮点数: 超过11位(用科学计数法表示有效数字超过11位的数)超过15位以0显示
        - 列宽过小: 用浮点数截取更少的有效数字, 如果
     2) 字符型: 左对齐
        - 默认: 输入同显示
        - 特殊: 数值以文本输入
        - 输入时设置: 在前面加英文单引号 或 =""
        - 提前设置: "开始"选项卡, "数字"组, 文本
        - 说明: 数字作为文本, 也可以进行算术计算
     3) 日期和时间: 右对齐
        - 分隔符: 日期(/或-) 时间(:) 日期和时间之间(一个空格)
        - 说明: 插入系统日期(Ctrl+;) 插入系统时间(Ctrl+Shift+;)
        - 当做数字处理: 距离1900年的天数(1900年的时间无效)
     4) 逻辑型: 居中对齐
        - 真/假: TRUE/False
        - 例1: =3=3 TRUE
        - 例2: =(3>5)+1 1

3. 自动填充

   - 使用填充柄填充

     | 初值为1个单元格 | 数值      | 日期和时间;数值+文字;自定义序列(星期/月/季/甲乙丙/子丑寅卯) |
     | --------------: | :-------- | ----------------------------------------------------------- |
     |            拖动 | 复制      | 等差(天/小时;数值递增/递减,文字不变的自然语言规律)          |
     |       Ctrl+拖动 | 等差      | 复制                                                        |
     |        右键拖动 | 快捷菜单: | 快捷菜单: (复制单元格/填充单元格/仅填充格式/不带格式填充)   |

     初值为等差/等比数列:

     1) 拖动默认按等差填充;
     2) Ctrl+拖动为复制
     3) 右键拖动, 出现快捷菜单, 可选择

   - "开始"选项卡, "编辑"组, 填充, 系列

     在A1:A5中, 输入1.3.5.7.9序列的方法:

     在A1、A2单元格中，分别输入1和3，选中A1:A2并指向填充柄，按下鼠标左键向下拖动填充柄到A5。

### 2.4 单元格 其他操作

- 选定、移动或复制数据、插入、删除、清除单元格
- 通用方法：
  1. 选中，单击右键
  2. 开始选项卡，剪贴板组(移动或复制数据)/单元格组(插入、删除)/编辑组(清除单元格)
  3. 快捷键
- 说明：
	1. Excel 单元格移动时，鼠标的形状为空心箭头指向左上角。⭐
	2. 清除与删除⭐⭐
		- 清除：1)全部清除 2)清除格式 3)清除内容 4)清除批注 5)清除超链接
		- 删除：1)右侧单元格左移 2)下侧单元格上移 3)整行 4)整列
		- 快捷键：1)delete：*清除选中区域内容* 2)backspace：*清除选中区域中，活动单元格的内容；删除没有快捷键*

## 3 数据美化

### 3.1 局部美化

#### 3.1.1 设置单元格格式

1. 方法：
	1. 开始选项卡，单元格组，格式⭐
	2. 右击单元格⭐
	3. 开始选项卡，字体组/对齐方式组/数字组⭐⭐

2. 针对字体组/对齐方式组/数字组说明：
	1. 字体：加边框、填充颜色(底纹)⭐
	2. 对齐方式⭐
	  1. 垂直对齐方式：靠上 居中 靠下 两端 分散
	  2. 水平对齐方式：左对齐 居中对齐 右对齐 两端对齐 分散对齐 常规 跨列居中 填充
	  3. 合并后居中(多个连续单元格合并，保留左上角第一单元格内容)⭐
	  4. 自动换行
	3. 数字组：
	  1. 小数位数：四舍五入⭐⭐
	  2. 百分比：%⭐⭐
	  3. 货币符号：¥⭐
	  4. 千位分隔符：,⭐
	
#### 3.1.2 改变行高、列宽

- 方法：
	1. 大致调整：拖动行列分隔线⭐(列调节右侧分割线；行调节下侧分隔线)
	2. 自动调整：双击行列分隔线⭐⭐
	3. 精确调整：右击行号/列号
	4. 精确调整：开始选项卡，单元格组，格式，行高/列宽
- 说明⭐
	1. 整行的高度是一样的
	2. 一次可以调整多行的行高
	3. 单个字符的字号会影响行高和列宽；但行高列宽反过来不会影响字符的字号。默认情况下，行高自动以本行中最高的字符为准
	4. 行高/列宽设置为0，该行/列将被隐藏，行号/列号将被保留

#### 3.1.3 套用表格格式/单元格样式/条件格式⭐

1. 表格格式/单元格样式：开始选项卡，样式组，套用表格格式/单元格样式(按题目要求照抄)
2. 条件格式(设置好条件格式后，更改数值，条件会自动变)：
	- 满足 xxx 条件，设置为 xxx 格式*(如果性别为男，则设置为蓝色底纹)*：开始选项卡，样式组，条件格式，突出显示单元格规则，xxx

#### 3.1.4 查找和选择

- 开始选项卡：编辑组⭐
- 查找范围：
	- 工作簿、工作表、按行、按列(无按单元格查找)
	- 数值、公式、批注(无按图查找)

#### 3.1.5 新建批注

- 审阅选项卡，批注组⭐
- 标记：单元格右上角，红色标记
- 默认不显示，不打印(可设置：页面设置对话框)

### 3.2 整体美化(页面美化)

#### 3.2.1 添加背景⭐

页面布局选项卡，页面设置组，背景(只能设置背景图)

#### 3.2.2 网格线⭐

- 设置方法：
	1. 页面布局选项卡，工作表选项组，网格线(默认：显示、不打印(可设置))
	2. 视图选项卡，显示组，网格线(只可设置是否显示)
	3. 文件选项卡，打印，页面设置对话框(只可设置是否打印)

## 4 数据计算

### 4.1 公式

1. 标记：以 = 开头⭐
2. 构成：常量、运算符、单元格地址、函数⭐例如：*(=2xA1 + SUM(A2:C2))*
3. 说明⭐⭐：单元格内输入公式：格上显示结果，底层显示公式；要查看公式，可以直接双击，可以单击后看编辑栏，还可以单击后按快捷键 F2

### 4.2 运算符

1. 引用运算符⭐⭐

  | 引用运算符 | 含义         |
  | ---------- | ------------ |
  | `:`        | 矩形选集     |
  | `,`        | 并集         |
  | `空格`     | 交集         |
  | `!`        | 三维引用     |
  | `[]`       | 跨工作表引用 |

2. 算数运算符：+ - * / ^ % -⭐(优先级：左高右底)

3. 文本连接运算符：&⭐⭐

4. 关系(比较)运算符：< <= > >= <> =

- 说明(优先级)：引用、算术、连接、比较

### 4.3 引用⭐⭐

1. 相对引用：公式复制，地址改变。如：A1
	C9=D5+F7 -> E7=*~~F3+H5~~*
2. 绝对引用：公式复制，地址不变。如：$A$1
3. 混合引用：$A1/A$1

- F4: 地址切换功能快捷键

- | D1       | E3       |
	| -------- | -------- |
	| =A1+A2   | =B3+B4   |
	| =A1+$A$2 | =B3+$A$2 |
	| =A1+$A2  | =B3+$A4  |

- 公式移动，地址不变

### 4.4 函数

- 插入函数的方法：⭐
	1. 编辑栏左侧的 f(x)
	2. 开始选项卡，编辑组，自动求和(Alt+=)
	3. 公式选项卡，函数库组
- 常用函数：
	1. 求和：SUM()
	2. 平均值：AVERAGE()
	3. 最大值：MAX()
	4. 最小值：MIN()
	5. 计数：COUNT()
		- 如果参数为引用单元格，只记录数值(右对齐)类型
		- 如果参数为
	6. 条件：IF()
- #操作/第四章  操作示例：计算⭐⭐
	
	- 版本：Office 2010
	- 选中(第一个写计算结果的单元格)，输入公式=XXXX，并按下回车键；选中XX并指向填充柄，按下左键向下/向右拖动填充柄到(最后一个写计算结果的单元格)
	
- 错误信息：

  - | 出错信息   | 可能的原因         |
    | ---------- | ------------------ |
    | `#DIV/0!`⭐ | 除数使用了零       |
    | `#NAME？`  | 不能识别名字       |
    | `#NUM!`    | 使用了无效数字     |
    | `#REF!`    | 引用了无效的单元格 |
    | `#VALUE`   | 引用时参数类型有错 |
    | `#NULL!`   | 区域交集为空       |
    | `#N/A`     | 没有可用数值       |

## 5 数据管理

- 数据清单(记录单)：
	1. 作用：用于进行排序、筛选、分类汇总等操作⭐⭐
	2. 构建要求⭐| 第一行：列标题 | 同一列：数据类型要相同 | 避免出现空白的行或列 | 尽量在一张工作表上
	3. (从数据库引申出来的)行叫记录，列叫字段；列标题叫字段名
- 操作方法：
	1. 数据选项卡：排序和筛选组(排序、筛选) 数据工具组(数据有效性、分列、合并计算) 分级显示组(分类汇总)
	2. 开始选项卡：编辑组(排序和筛选)

### 5.1 排序

1. 简单排序：一个关键字

	可选排序列某一格(不选排序列)

2. 复杂排序：

	- 64 个关键字：主要、次要关键字⭐⭐(Word中最多三个关键字)
	- 次序：升序、降序、自定义序列⭐⭐
	- 排序依据：数值、颜色(字/格)、单元格图标等⭐
	- 排序列有空格：排在最后⭐⭐
	- 排序列有相同值：次序不变
	- #操作/第四章 操作示例:
		版本：Office 2010
	
		选中XXX(带列标题的全部有效区域)，单击数据选项卡，排序和筛选组，排序；设置主要关键字为XXX，次序为XXX;次要关键字为XXX，次序为XXX。

### 5.2 筛选

1. 满足条件-显示，不满足条件-隐藏⭐⭐

2. 可按值、颜色(字/格)、单元格图标等筛选(同排序)⭐

3. 分类：自动筛选、高级筛选

	| ⭐⭐     | 自动筛选   | 高级筛选                                                     |
	| ------ | ---------- | ------------------------------------------------------------ |
	| 不同列 | 与         | 与、或                                                       |
	| 说明   | 还可以排序 | 需要先设置条件区域 1.同行与；不同行或 2.默认为绝对引用 3.字段名必须同原表 |

	#操作/第四章  操作示例⭐⭐自动筛选

	版本：Office 2010
	选中XXX(带列标题的全部有效区域)，单击数据选项卡，排序和筛选组，筛选；单击XXX字段筛选器箭头，设置筛选条件为XXX

### 5.3 分类汇总

例：汇总男女生语文成绩、数学成绩平均分
分类字段：性别
汇总项：语文成绩、数学成绩
汇总方式：平均值

1. 分类：分类汇总前，对分类字段排序⭐⭐
2. 汇总：自动计算-分类与总计⭐
   (结果可以展开或折叠)
3. 全部删除按钮，可以撤销分类汇总⭐

#操作/第四章  操作示例⭐⭐分类汇总
版本：Office 2010
按分类字段排序(方法见排序)；选中XXX，单击数据选项卡，分级显示组，分类汇总；设置分类字段为XXX(1个)，汇总方式为XXX(1种)，选定汇总项为XXX(多个)。

### 5.4 数据有效性⭐

输入信息提示、出错警告、圈释无效数据(红色椭圆)

说明：典型示例
*百分制成绩：小数/整数，介于，最小值为0，最大值为0*
*十位学号：文本长度，等于，10*
*性别：序列，来源：男、女*

#操作/第四章  操作示例：设置高数英语为百分制成绩有效
版本：Office 2010
选中(不带列标题的有效数据区域)；单击数据选项卡，数据工具组，数据有效性；设置XXX

### 5.5 分列

将一列的内容(如姓和名)拆分到不同的列中：1.固定宽度 2.分隔符号

### 5.6 合并计算

把多个区域的数据合并到一起
1.按位置合并：数据源表结构相同
2.按类别合并：结构不同(标签：首行、最左列)

## 6 图表

1. 图表: 数据的图形化表示⭐
2. 图表数据: 可相邻或不相邻(Ctrl)⭐⭐
3. 图表分类: 1)嵌入式图表 2)图表工作表(Chart1...)⭐⭐(F11:蔟状柱形图)*(Shift+F11:向前插入图表)*
4. 图表类型: 4 2 2 2⭐
	- 四(掌握): 柱形图 条形图 折线图(看变化趋势) 饼图(看比例)
	- 三: 圆环图 气泡图 散点图
	- 二(面): 曲面图 面积图
	- 二(特): 股价图 雷达图
5. 说明: 图表考点
	1) 图表可移动、复制、删除、可改位置、大小⭐⭐
	2) 数据源改变 -> 图表自动变⭐⭐
		图表中的数据删除 -> 数据源表不变
	3) 分类汇总后的数据生成图表, 当数据源展开或折叠时, 图表数据会自动显示或隐藏⭐
	4) 图表可单独打印⭐
6. 图表修饰⭐⭐ (插入: 插入选项卡, 表格组; 设置: 图表工具-设计/布局/格式选项卡)
	1) 图表工具-设计(整体): **图标样式**(如: 样式1)、**图表布局**(如: 布局1); 增加、删除图表数据; 切换行、列; 移动图表
	2) 图表工具-布局(局部): **标签组**(图表标题、坐标轴标题、图例、数据标签等)、坐标轴组(坐标轴、网格线)、背景组等
	3) 图表工具-格式选项卡: 高度/宽度

#操作/第四章  操作示例: 插入图表⭐⭐
选中XXX(带列标题第一列有效数据), 按下Ctrl键同时, 选中XXX(带列标题的其他列有效数据); 单击插入选项卡, 图表组, XXX(大类, 如:柱状图), 选择XXX(小类, 如:三维蔟状柱形图)

#操作/第四章  操作示例: 插入图表⭐⭐
选中图表, 单击图表工具XXX选项卡, XXX组, 设置XXX

###### 说明 1: 迷你图⭐
1) 数据源: 一列或一行
2) 绘图区域: 一个单元格
3) 创建迷你图: 插入选项卡, 迷你图组
4) 类型: 折线图, 柱形图, 盈亏图

###### 说明 2: 数据透视表/图
1) 插入选项卡, 表格组, 数据透视表⭐
2) 功能: 一次完成美化、计算、排序、筛选(图表)等操作, 有利于将数据转换为有价值的信息。⭐⭐
3) 数据源变 -> 透视表/图默认不变 -> 可刷新⭐

## 7 页面设置与打印

说明: 
1)打印: 工作簿、工作表(默认), 选定区域、图表⭐⭐
2)一行/一列数据可以在多页纸上打印⭐
3)页面设置对话框⭐
  Excel中, 页眉页脚可设置(首页不同、奇偶页不同)、可编辑(Word中只能设置; PPT和Excel一样)
